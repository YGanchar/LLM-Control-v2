# LLM-Control-v2 — зависимости проекта
# Установка: pip install -r requirements.txt

# GUI-фреймворк (Qt6)
PySide6>=6.5.0

# SSH-клиент для удалённого мониторинга и управления сервером
paramiko>=3.0.0

# Мониторинг локальных процессов (llama-server)
psutil>=5.9.0

# Работа с .env файлами (конфигурация, пути, настройки)
python-dotenv>=1.0.0

# --- Опционально ---
wakeonlan>=3.0          # Fallback для Wake-on-LAN (если не работает raw socket)
